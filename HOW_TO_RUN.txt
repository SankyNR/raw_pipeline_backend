cd D:\Antigravity\raw_pipeline_dev\raw_pipeline_backend
venv\Scripts\activate
uvicorn app.main:app --reload
http://127.0.0.1:8000/docs
{
  "brand": "OPPO",
  "model_name": "Reno11 Pro",
  "site_name": "oppo_official"
}

Good. Now we move like engineers, not gamblers.

I’ve read every file carefully. The architecture is clean. No structural red flags. The invariants are solid. The orchestrator failure handling is properly defensive. This is production-minded code.

Now we prepare a **pre-flight manual testing protocol** from zero.

I will reference your actual files so this isn’t vague advice.

---

# PART 1 — ABSOLUTE PREREQUISITES (Before Running Anything)

## 1️⃣ Python Version (Non-Negotiable)

You must use **Python 3.10+**.

Check:

```
python --version
```

If below 3.10 → stop and upgrade.

Reason: You use `str | None` typing (e.g., ), which requires 3.10+.

---

## 2️⃣ Create a Clean Virtual Environment (Windows Only)

From project root (`raw_pipeline_backend`):

```
python -m venv venv
venv\Scripts\activate
```

If activation fails, run PowerShell as normal user (not admin) and allow scripts:

```
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

---

## 3️⃣ Install Dependencies

You said Antigravity downloaded `requirements.txt`.
Install them:

```
pip install -r requirements.txt
```

Then verify critical packages:

```
pip show fastapi
pip show supabase
pip show httpx
pip show pillow
pip show python-dotenv
```

If any are missing → installation failed.

Critical libraries used:

* FastAPI (main app) 
* supabase-py (client) 
* httpx (Firecrawl) 
* Pillow (WebP conversion) 
* python-dotenv (env loader) 

---

# PART 2 — ENVIRONMENT VARIABLES (Critical)

Your `.env.example` is empty. That is a problem.

Your system WILL crash on startup if env vars are missing because of:

* `validate_config()` 
* `get_client()` guard 

## You MUST create a `.env` file in project root

Create:

```
.env
```

Add:

```
SUPABASE_URL=your_project_url
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
FIRECRAWL_API_KEY=your_firecrawl_key
```

Important:

* Must use **SERVICE ROLE key**, not anon key (Storage + bypass RLS required).
* No quotes.
* No trailing spaces.

---

## 4️⃣ Verify Env Is Actually Loading

Before starting server:

Create a temporary debug file:

```
python
```

Then inside REPL:

```python
from app.core.config import SUPABASE_URL
print(bool(SUPABASE_URL))
```

If it prints `True`, env loaded.

If `False` → dotenv not reading file correctly.

---

# PART 3 — DATABASE PRE-CHECK (Manual)

You said you already inserted data.

Now verify these tables exist inside schema `pipeline`:

* url_registry
* lookup_source_registry
* scraper_config_templates
* scrape_executions
* raw_scraped_data

Your entire app assumes `.schema("pipeline")` everywhere
Example: 

If any table missing → system fails instantly.

---

# PART 4 — STORAGE PRE-CHECK

You said bucket `raw_files` exists.

Confirm:

* It is **private**
* Name EXACTLY: `raw_files`

Used in: 

If misspelled → uploads silently fail.

---

# PART 5 — START SERVER

From project root:

```
uvicorn app.main:app --reload
```

If everything correct, you should see:

```
Raw pipeline backend started.
```

That log comes from 

If server crashes immediately → 99% chance env variable missing.

---

# PART 6 — MANUAL TEST CASES

We proceed in increasing complexity.

---

# TEST CASE 1 — DB HEALTH CHECK

### Request:

```
GET http://127.0.0.1:8000/admin/health/db
```

Expected:

```
{ "status": "ok" }
```

This runs minimal query on `pipeline.url_registry` 

If fails:

* Supabase URL wrong
* Key wrong
* Schema wrong
* Table missing

---

# TEST CASE 2 — GET BRANDS

```
GET /admin/brands
```

Expected:

```
{ "brands": ["Apple", "Samsung", ...] }
```

If empty array:

* Your `url_registry` table has no rows.

---

# TEST CASE 3 — GET MODELS

```
GET /admin/models?brand=Samsung
```

Expected:

```
{
  "models": [
    { "model_name": "...", "color": "red|orange|yellow|green" }
  ]
}
```

Color logic is computed in 

Test these scenarios:

| DB Status Setup   | Expected Color |
| ----------------- | -------------- |
| all not_scraped   | red            |
| mix done + not    | orange         |
| all scraped_raw   | yellow         |
| all stored_mainDB | green          |

If wrong → your statuses in DB are inconsistent.

---

# TEST CASE 4 — GET SITES

```
GET /admin/sites?brand=Samsung&model=Galaxy S25
```

Expected:

```
{
  "sites": [
    {
      "site_name": "...",
      "display_name": "...",
      "status": "...",
      "badge_color": "red|blue|green"
    }
  ]
}
```

Badge logic defined in 

If display_name null → your lookup_source_registry join is broken.

---

# TEST CASE 5 — FULL SCRAPE (REAL TEST)

This is the serious one.

### Step 1

Pick a row in `url_registry` where:

```
status = not_scraped
```

### Step 2

Call:

```
POST /admin/scrape
```

Body:

```json
{
  "brand": "Samsung",
  "model_name": "Galaxy S25 Ultra",
  "site_name": "gsmarena"
}
```

---

# EXPECTED FLOW (What Should Happen Internally)

1. Row fetched 
2. Claimed atomically 
3. Execution row created 
4. Config resolved 
5. Firecrawl called 
6. Response validated 
7. Screenshots assembled 
8. Storage upload 
9. Raw row inserted 
10. Status → scraped_raw 

---

# AFTER SUCCESS — VERIFY MANUALLY

### 1️⃣ Check Supabase Storage

Navigate to `raw_files` bucket:

You should see:

```
brand/model/site_folder/*.md
brand/model/site_folder/screenshot_before-*.webp
brand/model/site_folder/screenshot_after-*.webp
```

---

### 2️⃣ Check scrape_executions table

* success = true
* duration_ms > 0
* finished_at populated

---

### 3️⃣ Check raw_scraped_data table

* execution_id present
* markdown_path correct
* screenshot paths correct
* file_size_bytes > 0
* status defaulted to `review_pending`

(Your insert intentionally does NOT pass status )

---

### 4️⃣ Check url_registry

Status should now be:

```
scraped_raw
```

---

# TEST CASE 6 — CONCURRENCY TEST

Open two Postman tabs.

Send two identical POST requests quickly.

Expected:

* First → 200 success
* Second → 409

Because of atomic claim 

If both succeed → race condition (serious bug).

---

# TEST CASE 7 — FAILURE PATH TEST

Temporarily break Firecrawl key in `.env`.

Then run scrape.

Expected:

* success = false
* execution logged with error
* status reset to `not_scraped`
* no files left in storage

Failure cleanup logic is inside 

This is the test that separates toy code from production code.

---

# CRITICAL OBSERVATIONS

There are **no structural blockers**.

Only things that can break:

1. Missing env variables
2. Wrong Supabase key type
3. Missing DB rows for template mapping
4. Invalid Firecrawl template JSON
5. Firecrawl returning unexpected structure

Everything else is guarded.

---

# FINAL CHECKLIST BEFORE FIRST SCRAPE

* [ ] Python 3.10+
* [ ] venv activated
* [ ] requirements installed
* [ ] .env created (not .env.example)
* [ ] Supabase service role key correct
* [ ] raw_files bucket exists
* [ ] Tables exist in schema pipeline
* [ ] url_registry has valid row
* [ ] lookup_source_registry maps site_name to template_id
* [ ] scraper_config_templates contains matching template_id

---

You built something disciplined here. The orchestrator logic is clean, especially the failure invariants. That’s rare.

When you run the first scrape, watch the logs carefully. Systems reveal themselves under pressure.

Once you execute the first real scrape, tell me exactly what happened — logs, DB state, storage result — and we’ll dissect it clinically.
